V5_SPEC.md

改訂日：2026-06-24
Version：Ver.1.1

本書はV5システム全体の最上位仕様書であり、各詳細仕様書（DATA_SPEC、DIRECTORY_STRUCTURE等）は本書を基準とする。

⸻

# hiro-san 山歩きサイト V5 仕様書

## V5アーキテクチャ原則

V5は GitHub Pages + Jekyll を利用した山行記録サイトである。

本システムは、表示用途ごとにデータソースを明確に分離するデータ駆動型アーキテクチャを採用する。

records系

* site.posts を唯一のデータソースとする。
* records_master.json を直接参照してはならない。
* 山行記事一覧および個別記事を表示する。

logs系

* records_master.json
* SBrecords.json
* STrecords.json

以上3つのJSONを直接利用する。

* site.posts は利用しない。
* 横断検索・山行データ検索を担当する。

## JSONデータの役割

データ	用途
records_master.json	hiro-san 山行データのマスターデータ

SBrecords.json	SilverBoy 山行データ

STrecords.json	ショウタン 山行データ

records_master.json は hiro-san の山行データを管理する唯一のマスターデータ（Single Source of Truth）とする。
Markdown は本データから自動生成される派生データであり、山行データの追加・修正は records_master.json を更新して行う。

## Markdown生成

Markdown化の対象は records_master.json のみとする。

SBrecords.json および STrecords.json は Markdownへ変換しない。

最重要ルール

* records/index は site.posts を利用する。
* logs/index は JSON を直接検索する。
* records系と logs系のデータソースを混在させない。
* この設計は V5 の中核仕様であり、変更してはならない。

⸻

# 概要

V5は GitHub Pages + Jekyll により構築する山行記録サイトである。

データ駆動型設計を採用し、以下の3つのJSONファイルを中心に運用する。

* records_master.json
* SBrecords.json
* STrecords.json

用途ごとにデータ取得方法を分離し、保守性・拡張性・実装の単純化を実現する。

⸻

## データフロー

hiro-san データ（CSV or EXCEL）

records_master.json

        │
        ▼
 Markdown生成
 
        │
        ▼
     _posts
     
        │
        ▼
   site.posts
   
        │
        ├── records/index
        └── record.html

⸻

## 共同管理データ

- records_master.json
- SBrecords.json
- STrecords.json
  
        │
        ▼
   JSONを直接利用
  
        │
        ▼
    logs/index

⸻

# ページ構成

## 主要ページ

* index.html
* records/index.html
* record.html
* logs/index.html
* blog/index.html
* about/index.html
* other/index.html

## 将来拡張ページ

* area/index.html（未定）
* genre/index.html（未定）

これらのページについても、本仕様書で定義するV5アーキテクチャ原則に従って実装する。

# アーキテクチャ概要

V5では、ページの役割に応じてデータ取得方法を明確に分離する。

| 区分       | データソース                                                | 主な用途               |
| -------- | ----------------------------------------------------- | ------------------ |
| records系 | site.posts                                            | 山行記事一覧、絞り込み、個別記事表示 |
| logs系    | records_master.json / SBrecords.json / STrecords.json | 山行データ横断検索          |

records系とlogs系は、それぞれ独立したデータ取得方式を採用する。

---

# records/index

## 目的

records/index は、hiro-san の山行記録を写真付きカード形式で閲覧するための一覧ページとする。

サイト内における山行データベースの入口として機能する。

---

## データソース

利用するデータソースは `site.posts` のみとする。

### データ生成フロー

```text
records_master.json
        │
        ▼
 Markdown生成
        │
        ▼
     _posts
        │
        ▼
   site.posts
        │
        ▼
 records/index
```

records_master.json を直接検索してはならない。

---

## データ取得

Liquid により `site.posts` を取得し、JavaScript によるクライアントサイド処理で一覧表示・絞り込みを行う。

records/index は全文検索ページではなく、site.posts を一覧表示し、条件による絞り込みを行うページとする。

---

## カード表示項目

各カードには以下を表示する。

* tm（サムネイル画像）
* title
* date_s
* area
* genre
* sm2（山行概要）

---

## カード表示順

1. サムネイル画像（tm）
2. タイトル
3. 山行日
4. エリア
5. ジャンル
6. 山行概要

---

## 絞り込み

### 対象項目

* area
* genre
* date_s（年）

### 対象外

以下は検索対象としない。

* title
* members
* weather
* summary
* course_time
* course_note

---

## 検索UI

設置するUIは以下のとおりとする。

* エリア
* ジャンル
* 年
* 表示件数

プルダウンの選択肢は固定値ではなく、`site.posts` のデータから動的に生成する。

---

## 並び順

標準表示は `date_s` の降順（新しい順）とする。

ソート変更機能は設けない。

---

## ページネーション

### 初期表示件数

20件

### 表示件数

以下から選択可能とする。

* 5件
* 10件
* 15件
* 20件
* 25件
* 30件
* 35件
* 40件
* 45件
* 50件

5件刻みで変更可能とする。

---

## 空データの扱い

以下のルールを適用する。

* データが存在しない項目は表示しない。
* サムネイル画像が存在しない場合は画像領域を表示しない。
* sm2 が空欄の場合は概要を表示しない。

---

## ワイヤーフレーム

```text
絞り込み

エリア [▼]
ジャンル [▼]
年 [▼]
表示件数 [▼]

──────────────

サムネイル

タイトル

山行日
エリア
ジャンル

山行概要

──────────────

（カード一覧）

──────────────

ページネーション
```

---

## 備考

records/index は写真付き一覧ページであり、全文検索は行わない。

山行タイトルや概要文による検索は logs/index が担当する。

# record.html

## 目的

record.html は、hiro-san の山行記録を閲覧するための個別ページとする。

本ページは**山行データシート**として機能し、Markdown本文を主コンテンツとはしない。

詳細な山行記録は `yamareco_url` への導線によって提供する。

---

## データソース

各記事の Front Matter を利用する。

Front Matter は `records_master.json` から生成された Markdown ファイルに含まれるデータを使用する。

利用するデータソースは site.posts のみとする。records_master.json を直接参照してはならない。

---

## 基本方針

* 主コンテンツは Front Matter のデータおよび画像とする。
* `{{ content }}` は主コンテンツとして利用しない。
* 山行の詳細は `yamareco_url` を参照する。
* 情報の一覧性と視認性を重視したレイアウトとする。

---

## 表示項目

### メタ情報

* title
* date_s
* date_e
* duration
* area
* genre
* members
* weather

---

### メイン表示

* tm（メイン画像）
* tmfg（画像キャプション）
* sm2（山行概要）

---

### コース情報

* course_time
* course_note

---

### フォトギャラリー

以下の画像を表示対象とする。

* gp1
* gpfg1
* gp2
* gpfg2
* gp3
* gpfg3

---

### 外部リンク

* yamareco_url
* map

---

### ナビゲーション

ページ下部に以下を表示する。

* 前の記事
* records一覧へ戻る
* 次の記事

---

## 表示順序

1. パンくずリスト
2. タイトル
3. メタ情報
4. メイン画像
5. メイン画像キャプション
6. 山行概要
7. コース情報
8. フォトギャラリー
9. 外部リンク
10. ナビゲーション

表示順はワイヤーフレームを基準とする。CSS によるレイアウト変更は妨げない。

---

## 表示ルール

### メタ情報

項目が存在するもののみ表示する。

---

### メイン画像

`tm` が存在する場合のみ表示する。

`tmfg` はキャプションが存在する場合のみ表示する。

---

### 山行概要

`sm2` が存在する場合のみ表示する。

---

### コース情報

`course_time` または `course_note` のいずれかが存在する場合のみ、コース情報セクションを表示する。

両方とも空欄の場合はセクション全体を表示しない。

---

### フォトギャラリー

画像が存在するもののみ表示する。

すべての画像が空欄の場合は、フォトギャラリーセクション全体を表示しない。

各キャプション（gpfg1～gpfg3）は対応する画像が存在し、かつキャプションが入力されている場合のみ表示する。

---

### 外部リンク

`yamareco_url` および `map` は、それぞれ存在する場合のみ表示する。

両方とも存在しない場合は、外部リンクセクションを表示しない。

---

## ワイヤーフレーム

```text
パンくず

タイトル

────────────────

メタ情報

────────────────

メイン画像

メイン画像キャプション

────────────────

山行概要

────────────────

コース情報

────────────────

フォトギャラリー

────────────────

外部リンク

────────────────

前の記事 ｜ 一覧へ戻る ｜ 次の記事
```

---

## 空データの扱い

以下の共通ルールを適用する。

* データが存在しない項目は表示しない。
* ラベルのみを表示しない。
* 画像が存在しない場合は画像領域を表示しない。
* キャプションのみを表示しない。
* セクション内の全項目が空欄の場合は、そのセクション全体を表示しない。

---

## 備考

record.html は、山行記事ではなく**山行データシート**として位置付ける。

詳細な山行記録はヤマレコを参照し、本ページでは山行概要・基本情報・写真を中心に表示する。

Markdown本文は将来的な補足情報や拡張用途として保持するが、通常のページ表示では主コンテンツとして使用しない。

# logs/index

## 目的

logs/index は、hiro-san・SilverBoy・ショウタンの山行データを横断検索するページとする。

records/index が写真付き一覧ページであるのに対し、logs/index はテキスト主体の検索・閲覧ページとして位置付ける。

---

## データソース

以下のJSONファイルを直接読み込む。

* records_master.json
* SBrecords.json
* STrecords.json

`site.posts` は利用しない。

---

## データ取得

JavaScript により、ユーザーが選択したJSONファイルのみを読み込み、検索対象データを生成する。

検索対象として選択できるJSONファイルは以下とする。

* records_master.json
* SBrecords.json
* STrecords.json

1つまたは複数のデータソースを選択できる。

Markdownファイルや `site.posts` を経由してはならない。

---

## 検索対象

### 検索対象データ

検索を実行する前に、検索対象となるデータソースを選択する。

選択対象は以下とする。

* records_master.json
* SBrecords.json
* STrecords.json

チェックボックス形式とし、複数選択を可能とする。

選択されたデータソースのみを検索対象とする。

データソースが1件も選択されていない場合は検索を実行しない。

---

### 検索対象項目（キーワード検索）

検索対象として選択されたデータソースに対し、以下の項目を対象としてキーワード検索を行う。

* title
* summary

### 検索対象項目（プルダウンリスト）

- area
- genre
- year （date_sから生成）
- month （date_sから生成）

---

## 検索方式

検索は次の手順で実行する。

1. 検索対象データを選択する。
2. キーワード・絞り込み条件を入力する。
3. 「検索」ボタンを押して検索を実行する。

リアルタイム検索は採用しない。

---

## 検索フォーム

### 入力項目

* 検索対象データ（チェックボックス）
* キーワード
* エリア
* ジャンル
* 年
* 月
* 検索ボタン

---

### レイアウト

#### PC

```text
検索対象

☑ records_master.json
☑ SBrecords.json
☐ STrecords.json

キーワード

[________________________]

エリア[▼] ジャンル[▼] 年[▼] 月[▼]

[検索]
```

#### スマートフォン

画面幅に応じて自動的に折り返して表示する。

---

## 検索結果ヘッダー

### 初期表示

```text
検索結果：○○件 / 全件表示
```

### 検索後

```text
検索結果：○○件

条件：
北アルプス ｜ 縦走 ｜ 2025年
```

### 表示ルール

未指定の条件は表示しない。

例

```text
条件：
槍ヶ岳 ｜ 北アルプス
```

---

## 検索結果表示

### 表示形式

一覧形式とする。

### スマホの場合

スマートフォン表示における条件検索後の画面挙動は以下の通りとする。

検索条件を入力し「検索」ボタンを押下した場合、検索条件パネルは画面上から一時的に非表示とする。
代わりに、現在適用中の検索条件を画面上部にサマリー表示する。

サマリー表示には、例えば以下のように実際に適用されている条件を明示する。

keyword="雪山"　genre="沢登り"

複数条件がある場合は、すべての適用中条件を一覧形式で表示する。

この状態では検索結果の閲覧に集中できるUIとする。

再度条件を変更して検索を行う場合は、「リセット」ボタンを押下することで検索条件サマリーを解除し、検索条件パネルを再表示する。このとき入力値も初期状態へ戻す。

本仕様により、スマホ環境において検索状態の可視性と操作性を両立させるものとする。

---

### 表示項目

#### 1行目

タイトル

#### 2行目

* 山行日
* エリア
* ジャンル
* データ種別

PCでは横並び表示とする。

画面幅が不足する場合は折り返しを許可する。

#### 3行目

summary の先頭約100文字を表示する。

---

## データ種別

検索結果には以下のいずれかを表示する。

* hiro-san
* SilverBoy
* ショウタン

データ種別はJSONには保持しない。

読み込んだデータソースからJavaScriptで判定し、表示用の値として付与する。

---

## リンク仕様

検索結果カード全体をクリック可能とする。

クリック時は対象レコードの `yamareco_url` へ遷移する。

record.html への遷移は行わない。

---

## レスポンシブ対応

### PC

```text
タイトル

2025-10-12　北アルプス　縦走　hiro-san

summary先頭100文字
```

### スマートフォン

```text
タイトル

2025-10-12

北アルプス / 縦走

hiro-san

summary先頭100文字
```

可読性を優先し、情報量に応じて適切に改行する。

---

## 空データの扱い

以下のルールを適用する。

* データが存在しない項目は表示しない。
* summary が空欄の場合は概要を表示しない。
* データ種別は必ず表示する。
* 外部リンクが存在しないデータはクリック対象としない。

---

## 備考

logs/index は、複数の山行データを横断的に検索するためのページである。

写真表示や個別記事の閲覧を目的とせず、検索性と一覧性を優先した構成とする。

records/index と役割を明確に分離し、全文検索およびキーワード検索は logs/index が担当する。

# Markdown生成

## 目的

`records_master.json` のデータから Jekyll 用 Markdown ファイルを自動生成する。

生成された Markdown は `site.posts` のデータソースとなり、records系ページで利用する。

---

## 元データ

* records_master.json

---

## 出力先

```text
_posts/
```

---

## ファイル名

命名規則は以下とする。

```text
YYYY-MM-DD-<record_id>.md
```

### record_id

`yamareco_url` に含まれる記録IDを使用する。

例

```text
https://www.yamareco.com/modules/yamareco/detail-1234567.html
```

↓

```text
2025-08-15-1234567.md
```

---

## Front Matter

Front Matter には `records_master.json` の各項目を出力する。

データ項目の詳細は **DATA_SPEC.md** に従う。

---

## Markdown本文

Markdown本文（`{{ content }}`）は主コンテンツとして利用しない。

必要に応じて補足情報を記述できるが、通常のページ表示では表示対象外とする。

---

# データ仕様

## records_master.json

### 用途

* Markdown生成元データ
* records/index の表示データ
* record.html の表示データ
* logs/index の検索対象

hiro-san の山行データを管理するマスターデータとする。

---

## SBrecords.json

### 用途

SilverBoy の山行データを保持する。

### 制約

* Markdown化しない。
* `site.posts` に変換しない。
* logs/index の検索対象としてのみ利用する。

---

## STrecords.json

### 用途

ショウタンの山行データを保持する。

### 制約

* Markdown化しない。
* `site.posts` に変換しない。
* logs/index の検索対象としてのみ利用する。

---

# 共通仕様

## 対応環境

以下の表示環境に対応する。

* PC
* タブレット
* スマートフォン

---

## レスポンシブ方針

ページ幅に応じてレイアウトを自動調整する。

スマートフォンでは可読性を優先し、情報量に応じて改行および縦並び表示を行う。

---

## ページネーション

一覧ページ（records/index、logs/index）は共通仕様とする。

### 初期表示件数

20件

### 表示件数

5件～50件

5件刻みで変更可能とする。

---

## 並び順

一覧ページは `date_s` の降順（新しい順）を標準とする。

ソート変更機能は設けない。

---

## 空データの扱い

すべてのページで以下のルールを適用する。

* データが存在しない項目は表示しない。
* ラベルのみを表示しない。
* セクション内の全項目が空欄の場合はセクション全体を表示しない。
* 画像が存在しない場合は画像領域を表示しない。
* キャプションのみを表示しない。

---

## CSS

共通CSSを利用する。

ページ固有のスタイルは必要最小限とする。

---

# 開発ルール

## 基本方針

V5アーキテクチャの範囲内で実装を行う。

実装の都合によって設計を変更してはならない。

---

## 設計変更が必要な場合

実装前に提案を行うこと。

提案には以下を含める。

* 変更理由
* メリット
* デメリット
* 影響範囲

承認を得るまでは実装を行わない。

---

# V5設計原則（再確認）

実装時は以下の原則を必ず遵守する。

### records系

* site.posts を利用する。
* JavaScript によるクライアントサイド処理を行う。
* JSON を直接検索してはならない。

### logs系

* site.posts を利用しない。
* records_master.json
* SBrecords.json
* STrecords.json

以上3つのJSONを直接検索する。

### Markdown生成

Markdown化するのは `records_master.json` のみとする。

SBrecords.json および STrecords.json は Markdown化しない。

---

# 将来拡張

以下のページは将来追加を予定する。

* area/index.html
* genre/index.html

これらのページについても、本仕様書で定義するV5アーキテクチャ原則に従って実装する。

---

# 変更履歴

## Ver.1.1（2026-06-24）

* 文書全体の章構成を整理
* V5アーキテクチャ原則を新設
* record.html の仕様を一か所へ集約
* records/index と logs/index の責務を明確化
* Markdown生成仕様を独立章化
* 共通仕様を新設
* 開発ルールを整理
* 将来拡張方針を追加
* 重複記述を削除し、保守性を向上



